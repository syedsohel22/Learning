const initalState = {
  isLoading: false,
  isError: false,
  products: [],
};

export const reducer = (state = initalState, { type, paylaod }) => {
  switch (type) {
    default:
      return state;
  }
};
