import React from "react";

export const PinTab = ({ length, maxChar, setOtp }) => {
  return <div data-testid="pin-tab">{/* Add Pin component here  */}</div>;
};

// Check length and maxChar props here
