const initialState = {
  isLoading: false,
  isError: false,
  data: [],
};

export const reducer = (state = initialState, { action, type }) => {
  switch (type) {
    default:
      return state;
  }
};
